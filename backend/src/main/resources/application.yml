server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: crimenet

  # ── PostgreSQL ──
  datasource:
    url: jdbc:postgresql://localhost:5432/crimenet
    username: crimenet_app
    password: crimenet_app_dev
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          time_zone: UTC

  # ── Flyway ──
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    user: crimenet
    password: crimenet_dev

  # ── Keycloak (OAuth2 Resource Server) ──
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/crimenet
          jwk-set-uri: http://localhost:8180/realms/crimenet/protocol/openid-connect/certs

  # ── Redis ──
  data:
    redis:
      host: localhost
      port: 6379

  # ── RabbitMQ ──
  rabbitmq:
    host: localhost
    port: 5672
    username: crimenet
    password: crimenet_dev

# ── MinIO ──
minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin123
  sse-enabled: ${MINIO_SSE_ENABLED:false} # Standalone MinIO lacks KES; set to true in production with AWS S3 KMS or MinIO KES
  buckets:
    quarantine: crimenet-quarantine
    documents: crimenet-documents
    evidence: crimenet-evidence
    court-packages: crimenet-court-packages
    archive: crimenet-archive

# ── Document Upload Limits ──
crimenet:
  documents:
    max-size-bytes: 52428800 # 50 MB
    allowed-mime-types: application/pdf,image/jpeg,image/png,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document
  break-glass:
    expiry-check-ms: 60000
  provenance:
    anchor-interval-ms: 300000

# ── Blockchain Anchoring (Polygon Amoy Testnet / Local EVM) ──
blockchain:
  enabled: ${BLOCKCHAIN_ENABLED:true}
  rpc-url: ${BLOCKCHAIN_RPC_URL:https://polygon-amoy-bor-rpc.publicnode.com}
  private-key: ${BLOCKCHAIN_PRIVATE_KEY:0xb33ccddddcc977a89620e89e06d5f578f0f8e018d296cd361488d5bb9a19309d}
  contract-address: ${BLOCKCHAIN_CONTRACT_ADDRESS:0x6B76859551315E47041064F48E201b0c2E5832a7}

# ── OpenSearch ──
opensearch:
  host: localhost
  port: 9200
  scheme: http

# ── Actuator ──
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when_authorized

# ── OpenAPI / Swagger ──
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha

# ── Logging ──
logging:
  level:
    root: INFO
    com.crimenet: INFO
    org.springframework.security: INFO
